
To add the source code for any folder you created on your device first open git bash in that folder
Then after opening first of all use command to add all the files git add .
this will add all the files now it's time to commit the changes
use command git commit -m // -m shows message just like a normal string "changes added"
now
use the create a new repository in git hub and copy it's url 
then use commad git remote add origin and paste your copied url and enter
and then use command the final command git push origin master 
